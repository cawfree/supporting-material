import { Appear, Notes, Head, Image } from 'mdx-deck';
import { FullscreenCode, Split, SplitRight } from 'mdx-deck/layouts';
import { dark } from 'mdx-deck/themes';
import { CodeSurfer } from 'mdx-deck-code-surfer'
import Fade from 'mdx-deck-fade';
import Lottie from 'react-lottie';

export const theme = {
  ...dark,
  code: {
    ...dark.code,
    color: 'white',
    fontWeight: 'bold',
  },
  link: {
    ...dark.link,
    color: 'white',
    fontWeight: 'bold',
  },
};

import morphing from './static/lottie/morphing.json';
import floating from './static/lottie/floating.json';
import cogs from './static/lottie/cogs.json';
import graphs from './static/lottie/graphs.json';
import map from './static/lottie/map.json';
import lecture from './static/lottie/lecture.json';
import summary from './static/lottie/summary.json';

import ReactAdvanced from './components/ReactAdvanced';

<Head>
  <title>Threading in React Native: A bridge too far?</title>
</Head>

<ReactAdvanced>

<div
  style={{
    width: '100vw',
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 5,
    flexDirection: 'column',
  }}
>

# Threading in React Native: A bridge too far?

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: cogs,
    rendererSettings: {
      preserveAspectRatio: 'xMidYMid slice',
    },
  }}
  width="30vw"
  height="30vw"
/>

</div>

</ReactAdvanced>

---

<ReactAdvanced>

# Hey, 👋🏻 I'm Alex!

<img
  style={{
    borderRadius: 200,
    width: 400,
    height: 400,
    paddingTop: -20,
  }}
  src="./static/images/alex.png"
/>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: map,
  }}
  height="50vh"
/>

</ReactAdvanced>

<Notes
>

Creator of Mapsy.

3.5 Years RN.

Originally native Android.

Little web dev.

Lots of time to write terribly inoptimal algorithms and find all sorts of ways to try and fix them.

Which provides a lot of material for today's talk.

</Notes>

---

<ReactAdvanced>

## Context

<div
  style={{
    paddingLeft: 5,
  }}
>


  - **Lightning Talk ⚡ **
    - Is it possible to have truly threaded computation in React Native?

  - **State of the Field**
    - JavaScript is inherently _synchronous_.
    - Devices are becoming increasingly _parallel_.

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: lecture,
  }}
  height="50vh"
/>

</div>

</ReactAdvanced>

<Notes
>

Threading.

They would be useful, right?

Some complex functions just don't deserve to steal valuable processing time from React.

Leads to dropped render frames, application starts to jitter. (16ms)

Apple A13 Bionic CPU, 6 Core, (Developers)

</Notes>

---

<ReactAdvanced>

<div
  style={{
    width: '60vw',
    height: '100vh',
    padding: 10,
    alignItems: 'center',
    justifyContent: 'center',
    display: 'flex',
  }}
>

# Three Examples in Three Minutes

</div>

<div
  style={{
    position: 'absolute',
    top: 0,
    right: 0,
    width: '40vw',
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
  }}
>
  <Lottie
    options={{
      loop: true,
      autoplay: true,
      animationData: graphs,
      rendererSettings: {
        preserveAspectRatio: 'xMidYMid slice',
      },
    }}
    width="70vw"
    height="70vh"
  />
</div>

</ReactAdvanced>

<Notes
>

  - 

</Notes>

---

<ReactAdvanced>

<div
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100vw',
    height: '100vh',
  }}
>

  <div>

# ⚙️ Native Threads

  </div>

</div>

</ReactAdvanced>

<Notes
>
 - the beauty is any operation runtime, executable by react
</Notes>

---

<CodeSurfer
  title="Native Thread Example (Android)"
  code={require('./snippets/native.js')}
  lang="java"
  showNumbers={false}
  dark
  steps={[
    { notes: ""},
    { range: [15, 16], notes: "Export a native module." },
    { range: [21, 22], notes: "Allocate a thread pool."},
    { range: [24, 28], notes: "Expose a bridge method."},
    { range: [29, 41], notes: "Execute your operation. (Imagination!)"},
    { notes: "Highest performance, highest complexity."},
  ]}
/>

---

<ReactAdvanced>

<div
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100vw',
    height: '100vh',
  }}
>

  <div>

# 🖥️ The `<WebView/>`

  </div>

</div>

</ReactAdvanced>

<Notes
>
  - Every device has a browser
  - Execute the same javascript code, just on a different javascript thread
  - injects javascript to construct a communication bridge between rn and webview
</Notes>

---

<CodeSurfer
  title="<WebView /> Example"
  code={require('./snippets/webview.js')}
  lang="javascript"
  showNumbers={false}
  dark
  steps={[
    { notes: ""},
    { range: [4, 5], notes: "The <Elsewhere/> handles low-level serialization."},
    { range: [25, 36] },
    { range: [6, 14] },
    { range: [29, 33] },
    { notes: ""},
  ]}
/>

<Notes
>

DOM Storage

`<script />` dependencies

No compilation dependencies.

</Notes>

---

<ReactAdvanced>

<div
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '100vw',
    height: '100vh',
  }}
>

  <div>

# 🌪️ Hybrid Containers

  </div>

</div>

</ReactAdvanced>


---

<CodeSurfer
  title="Hybrid Example"
  code={require('./snippets/hybrid.js')}
  lang="javascript"
  showNumbers={false}
  dark
  steps={[
    { notes: ""},
    { range: [4, 4] },
    { range: [6, 6] },
    { range: [17, 20] },
    { range: [8, 14] },
    { range: [25, 32] },
    { notes: ""},
  ]}
/>

---

<ReactAdvanced>

## Summary

<div
  style={{
    width: '100vw',
    height: '100vh',
  }}
>

<div
  style={{
    position: 'absolute',
    width: '50vw',
    height: '50vh',
    paddingLeft: '10vw',
    paddingTop: '15vh',
  }}
>

  - **Use threads!**
    - They're too simple and too powerful to ignore.


  - **What are the options, again?**
    - ⚙️ Native Code
    -  🖥️ Delegate to a `<WebView />`
    - 🌪️ Hybrid Containers

</div>
<div
  style={{
    position: 'absolute',
    paddingTop: '12.5vh',
    width: '50vw',
    height: '50vh',
    right: 0,
  }}
>
<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: summary,
  }}
  height="50vh"
/>
</div>

</div>



</ReactAdvanced>

<Notes
>
Native high performance, dev/test complexity.

Web: simplistic and deprecated, browser fragmented, not truly multithreaded (common but dnu), cold start is slow with script dependencies

  - Hybrid: full-featured runtime, surprisingly simple, likely future

</Notes>

---

<ReactAdvanced>

<div
  style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    flexDirection: 'column',
    width: '100vw',
    height: '100vh',
  }}
>


# Thanks for listening!

## [@cawfree](https://twitter.com/cawfree) / [github.com/cawfree](https://github.com/cawfree)

</div>

</ReactAdvanced>

<Notes
>

</Notes>
