import {Appear, Notes, Head, Image} from 'mdx-deck';
import {FullscreenCode, Split, SplitRight} from 'mdx-deck/layouts';
import {notes} from 'mdx-deck/themes';
import {CodeSurfer} from 'mdx-deck-code-surfer'
import Fade from 'mdx-deck-fade';
import Lottie from 'react-lottie';

import {FontAwesomeIcon} from "@fortawesome/react-fontawesome";
import {faCrown, faBolt} from "@fortawesome/free-solid-svg-icons";

import {Layout} from "./components";

export const theme = {
  ...notes,
  code: {...notes.code, fontWeight: 'bold'},
  link: {...notes.link, fontWeight: 'bold'},
  ul: {fontSize: '4vh'},
  p: {fontSize: '16px'},
};

<Head>
  <title>React Native White Label</title>
</Head>

<Layout>


# white label react native

<div
  style={{
    fontSize: "4vh",
  }}
>
  {"where config is king "}
  <FontAwesomeIcon icon={faCrown} />
</div>

</Layout>
---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/puppy.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/developer.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="60vw"
  height="30vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/happy.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="60vw"
  height="60vw"
/>

</Layout>

---

<Layout
  style={{
    flexDirection: "row",
  }}
>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/design.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/react.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

</Layout>

---

<Layout
>

  # what is a white label app?

</Layout>

---

<Layout
>

  ## an app whose presentation

  <span
    style={{
      fontSize: "8vh",
      fontWeight: "bold",
    }}
  >
    {"is mostly api defined "}
    <FontAwesomeIcon icon={faBolt} />
  </span>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/money.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="90vw"
  height="30vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/graph.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="40vw"
  height="30vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/cloud.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="50vw"
  height="50vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/sushi.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="50vw"
  height="50vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/strategy.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="33vw"
  height="50vw"
/>

</Layout>

---

<Layout>

## SELECT * FROM tbl_style WHERE

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/dba.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

</Layout>

---

<Layout
  style={{
    flexDirection: "row",
  }}
>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/heart.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/react.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="30vw"
  height="30vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/code.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="60vw"
  height="60vw"
/>

</Layout>

---

<Layout>

<Lottie
  options={{
    loop: true,
    autoplay: true,
    animationData: require("./assets/programming.json"),
    rendererSettings: {preserveAspectRatio: 'xMidYMid slice'},
  }}
  width="60vw"
  height="60vw"
/>

</Layout>

---

<Layout>

# okay i get it, so styling is easy

</Layout>

---

<Layout>

# awesome demo

</Layout>


